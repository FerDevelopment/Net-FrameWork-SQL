<UserControl x:Class="DI.Proyecto.Clase._2024.Frontend.ControlUsuario.UCCrearUsuario"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DI.Proyecto.Clase._2024.Frontend.ControlUsuario"
             xmlns:mat="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid x:Name="dgCrearUsuario" CanUserAddRows="False" ColumnWidth="*" ItemsSource="{Binding listaUsuarios}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}"/>
                <DataGridTextColumn Header="Rol" Binding="{Binding Rol}"/>
                
                <DataGridTemplateColumn Header="Grupo - Departamento">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate >
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding GrupoNavigation.Nombre}"/>
                                <TextBlock Text="{Binding DepartamentoNavigation.Nombre}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Nombre - Apellidos">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate >
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Nombre}"/>
                                <TextBlock Text=" - "/>
                                <TextBlock Text="{Binding Apellido1}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Editar - Borrar">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>

                            <StackPanel Orientation="Horizontal">
                                <Button Content="{mat:PackIcon Kind=Edit}" Foreground="DodgerBlue" Style="{StaticResource MaterialDesignToolButton}"/>
                                <Button Content="{mat:PackIcon Kind=Delete}" Foreground="Red" Style="{StaticResource MaterialDesignToolButton}"/>
                            </StackPanel>

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


            </DataGrid.Columns>

            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Editar">
                        <MenuItem.Icon>
                            <mat:PackIcon Kind="Edit" Foreground="DodgerBlue"/>
                        </MenuItem.Icon>
                    </MenuItem>

                    <MenuItem Header="Borrar">
                        <MenuItem.Icon>
                            <mat:PackIcon Kind="Delete" Foreground="Red"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>  
    </Grid>
</UserControl>

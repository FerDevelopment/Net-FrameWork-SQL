<UserControl x:Class="DI.Proyecto.Clase._2024.Frontend.ControlUsuario.UCCrearArticulo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DI.Proyecto.Clase._2024.Frontend.ControlUsuario"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:mat="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid x:Name="dgCrearArticulo" ItemsSource="{Binding listaArmarios}" AutoGenerateColumns="False" ColumnWidth="*" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Número de serie" Binding="{Binding Numserie}"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}"/>
                <DataGridTemplateColumn Header="Fecha de alta">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <DatePicker SelectedDate="{Binding Fechaalta}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Usuario de alta" Binding="{Binding UsuarioaltaNavigation.Nombre}"/>
                <DataGridTextColumn Header="Nombre de modelo" Binding="{Binding ModeloNavigation.Nombre}"/>
                <DataGridTextColumn Header="Nombre de departamento" Binding="{Binding DepartamentoNavigation.Nombre}"/>
                <DataGridTextColumn Header="Espacio" Binding="{Binding EspacioNavigation.Nombre}"/>

                <DataGridTemplateColumn Header="Editar - Borrar">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            
                            <StackPanel Orientation="Horizontal">
                                <Button Content="{mat:PackIcon Kind=Edit}" Style="{StaticResource MaterialDesignToolButton}" Foreground="DodgerBlue" />
                                <Button Content="{mat:PackIcon Kind=Delete}" Foreground="Red" Style="{StaticResource MaterialDesignToolButton}" />
                            </StackPanel>
                            
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
            </DataGrid.Columns>

            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Editar">
                        <MenuItem.Icon>
                            <mat:PackIcon Kind="Edit" Foreground="DodgerBlue"/>
                        </MenuItem.Icon>
                    </MenuItem>

                    <MenuItem Header="Borrar">
                        <MenuItem.Icon>
                            <mat:PackIcon Kind="Delete" Foreground="Red"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>
            
        </DataGrid>  
    </Grid>
</UserControl>
